# If the app gets killed and is then reopened, it will ask to disable OpenGL
# and defaults back to software rendering, which is the only thing that works.
# timeout 1s Spriter
# sleep 1.5s
# Spriter

# But it turns out that's not necessary because I can just provide config files
Spriter